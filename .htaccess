<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect to www if not already www
    RewriteCond %{HTTP_HOST} ^seelenfluesterin\.ch$ [NC]
    RewriteRule ^(.*)$ https://www.seelenfluesterin.ch/$1 [L,R=301]

    # Force HTTPS
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Redirect all requests to the Seelenfluesterin public folder (except if already inside it)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]

    # Handle index.php properly
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /public/index.php [L]
</IfModule>